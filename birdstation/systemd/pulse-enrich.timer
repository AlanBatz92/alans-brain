# NOTE: schedule below is a best-guess — confirm against the installed unit
# (`systemctl cat pulse-enrich.timer`) and correct before cutover.
[Unit]
Description=Run Pulse enrichment a few minutes after each fetch

[Timer]
OnBootSec=5min
OnUnitActiveSec=15min
Persistent=true

[Install]
WantedBy=timers.target
